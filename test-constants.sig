sig test-constants.
accum_sig classical.

% Sample constants

type a,  b,  c,  d,  e, f, g, h       bool.
type r, t                        i -> bool.
type q, w                   i -> i -> bool.

type m, u, z                             i.
type s, k                           i -> i.

type wi, fi            i.
type is_fox, is_wolf   i -> bool.
type eats              i -> i -> bool.
